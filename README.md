# SZE — Game Portfolio

A game-focused portfolio featuring UX, art direction, and playtest-led design work.

## Run locally

Open `index.html` directly, or serve the folder with any static web server:

```sh
python3 -m http.server 8000
```
